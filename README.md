# ng-diff-highlight

Angular library for resolving canonical diff field paths, scoping those paths to a subtree, and applying deterministic highlight behavior for both editable and read-only UIs.

## Features

- Scoped diff state management.
- Canonical path resolution from DOM, Angular forms, and hierarchical path context.
- Automatic class toggling based on diff matches.
- Supports both standalone and NgModule-based Angular applications.
- Zero external UI dependencies (Angular and RxJS only).

## Documentation

Full documentation will be available soon. Refer to the `docs/` directory for architecture, API, and migration guides.

## License

MIT
